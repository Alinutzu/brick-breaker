// game.js — core gameplay, rendering, input
(function(){
  const W=800,H=600; const PADDLE_W=110,PADDLE_H=12,PADDLE_SPEED=540; const BALL_RADIUS=7; let INIT_BALL_SPEED=320; const BRICK_ROWS=10,BRICK_COLS=16,BRICK_W=42,BRICK_H=16,BRICK_GAP=2; let POWERUP_DROP_CHANCE=0.28; const MAX_BALLS=40;
  const state = { mode:'level', level:1, wave:1, paddle:null, balls:[], bricks:[], powerUps:[], score:0, lives:3, running:false, stageCleared:false, barrierRows: Math.floor(BRICK_ROWS*0.6), offsetX:0, corridor2Col:null, corridor2Opened:false, BRICK_W, BRICK_H, BRICK_GAP };
  const AudioCtx = window.AudioContext||window.webkitAudioContext; const audioCtx = new AudioCtx(); let audioEnabled = true; const soundBtn = document.getElementById('soundBtn');
  soundBtn.addEventListener('click', async ()=>{ audioEnabled=!audioEnabled; soundBtn.textContent = audioEnabled ? '\ud83d\udd0a Sunet: ON' : '\ud83d\udd07 Sunet: OFF'; if(audioCtx.state==='suspended' && audioEnabled){ try{ await audioCtx.resume(); }catch{} } });
  function beep(freq=440,duration=0.06,type='sine',vol=0.08){ if(!audioEnabled) return; try{ const o=audioCtx.createOscillator(); const g=audioCtx.createGain(); o.type=type; o.frequency.value=freq; g.gain.value=vol; o.connect(g); g.connect(audioCtx.destination); o.start(); setTimeout(()=>o.stop(),duration*1000); }catch(e){} }
  const sfx={ bounce(){beep(420,0.03,'square',0.07);}, brick(){beep(620,0.05,'triangle',0.08);}, power(){beep(880,0.08,'sine',0.09);}, lose(){beep(220,0.25,'sawtooth',0.08);}, win(){beep(980,0.20,'sine',0.1);} };
  function clamp(v,a,b){ return Math.max(a,Math.min(b,v)); } function randRange(a,b){ return a + Math.random()*(b-a); }
  function circleRectCollide(cx,cy,r,rx,ry,rw,rh){ const closestX=clamp(cx,rx,rx+rw); const closestY=clamp(cy,ry,ry+rh); const dx=cx-closestX; const dy=cy-closestY; return (dx*dx+dy*dy)<=r*r; }
  const particles=[];
  function spawnParticles(x,y,color,count=8){ for(let i=0;i<count;i++){ const ang=randRange(0,Math.PI*2); const speed=randRange(40,160); particles.push({x,y,vx:Math.cos(ang)*speed,vy:Math.sin(ang)*speed,life:0.35,alpha:1,color}); } }
  function updateParticles(dt){ for(let i=particles.length-1;i>=0;i--){ const p=particles[i]; p.x+=p.vx*dt; p.y+=p.vy*dt; p.vx*=0.92; p.vy*=0.92; p.life-=dt; p.alpha=Math.max(0,p.life/0.35); if(p.life<=0) particles.splice(i,1); } }
  function drawParticles(ctx){ for(const p of particles){ ctx.globalAlpha=p.alpha; ctx.fillStyle=p.color; ctx.fillRect(p.x-2,p.y-2,4,4); } ctx.globalAlpha=1; }
  class Paddle{ constructor(){ this.w=PADDLE_W; this.h=PADDLE_H; this.x=(W-this.w)/2; this.y=H-42; this.vx=0; this.wideTimer=0; this.wideMax=0; }
    update(dt,input){ this.vx=(input.right?1:0)-(input.left?1:0); this.x+=this.vx*PADDLE_SPEED*dt; this.x=clamp(this.x,0,W-this.w); if(this.wideTimer>0){ this.wideTimer-=dt; this.w=PADDLE_W*1.5; } else { this.w=PADDLE_W; } }
    draw(ctx){ ctx.fillStyle='#48c774'; ctx.fillRect(this.x,this.y,this.w,this.h); }
    rect(){ return {x:this.x,y:this.y,w:this.w,h:this.h}; }
  }
  class Ball{ constructor(x,y,speedMul=1){ this.x=x; this.y=y; const angle=randRange(Math.PI*0.25,Math.PI*0.75); const speed=INIT_BALL_SPEED*speedMul; this.vx=Math.cos(angle)*speed; this.vy=-Math.abs(Math.sin(angle)*speed); this.r=BALL_RADIUS; this.color='#f4f1c9'; this.pierceTimer=0; this.pierceMax=0; }
    update(dt){ this.x+=this.vx*dt; this.y+=this.vy*dt; if(this.x-this.r<0){ this.x=this.r; this.vx=Math.abs(this.vx); sfx.bounce(); } if(this.x+this.r>W){ this.x=W-this.r; this.vx=-Math.abs(this.vx); sfx.bounce(); } if(this.y-this.r<0){ this.y=this.r; this.vy=Math.abs(this.vy); sfx.bounce(); } }
    draw(ctx){ ctx.fillStyle=this.color; ctx.beginPath(); ctx.arc(this.x,this.y,this.r,0,Math.PI*2); ctx.fill(); }
  }
  class Brick{ constructor(x,y,w,h,hp=1,requiresPierce=false,indestructible=false,opensCorridor=false){ this.x=x; this.y=y; this.w=w; this.h=h; this.hp=hp; this.requiresPierce=requiresPierce; this.indestructible=indestructible; this.opensCorridor=opensCorridor; }
    draw(ctx){ let c='#7aa2ff'; if(this.indestructible){ c='#9aa0a6'; } else if(this.hp>1){ c='#ff7b7b'; } if(this.requiresPierce) c='#ffa500'; if(this.opensCorridor) c='#00d1b2'; ctx.fillStyle=c; ctx.fillRect(this.x,this.y,this.w,this.h); ctx.strokeStyle='#0c1229'; ctx.strokeRect(this.x+0.5,this.y+0.5,this.w-1,this.h-1); }
  }
  class PowerUp{ constructor(x,y,type){ this.x=x; this.y=y; this.type=type; this.w=14; this.h=14; this.vy=140; this.active=true; }
    update(dt){ this.y+=this.vy*dt; if(this.y>H+24) this.active=false; }
    draw(ctx){ if(!this.active) return; const colors={multiball:'#4da6ff',wide:'#ffd166',pierce:'#ef476f',split:'#47e5bc'}; ctx.fillStyle=colors[this.type]||'#aaa'; ctx.fillRect(this.x,this.y,this.w,this.h); }
    rect(){ return {x:this.x,y:this.y,w:this.w,h:this.h}; }
  }
  const canvas=document.getElementById('game'); const ctx=canvas.getContext('2d');
  const uiScore=document.getElementById('score'); const uiLives=document.getElementById('lives'); const uiStage=document.getElementById('stage'); const uiModeTag=document.getElementById('modeTag');
  const btnReset=document.getElementById('reset'); const btnPause=document.getElementById('pause'); const btnMenu=document.getElementById('menuBtn');
  const menu=document.getElementById('menu'); const grid=document.getElementById('levelGrid'); const btnChooseLevel=document.getElementById('chooseLevelMode'); const btnChooseEndless=document.getElementById('chooseEndless'); const btnCloseMenu=document.getElementById('closeMenu');
  const wideBox=document.getElementById('wideTimerBox'); const wideFill=document.getElementById('wideFill'); const pierceBox=document.getElementById('pierceTimerBox'); const pierceFill=document.getElementById('pierceFill');
  function showMenu(){ menu.hidden=false; menu.classList.remove('hidden'); input.paused=true; }
  function hideMenu(){ menu.hidden=true; menu.classList.add('hidden'); input.paused=false; }
  for(let i=1;i<=12;i++){ const b=document.createElement('button'); b.className='levelbtn'; b.textContent='Nivel '+i + (i===12?' ★':''); b.addEventListener('click',()=>{ setMode('level'); state.level=i; resetGame(); hideMenu(); }); grid.appendChild(b); }
  btnMenu.addEventListener('click',()=>{ showMenu(); }); btnCloseMenu.addEventListener('click',()=>{ hideMenu(); }); btnChooseLevel.addEventListener('click',()=>{ setMode('level'); resetGame(); hideMenu(); }); btnChooseEndless.addEventListener('click',()=>{ setMode('endless'); resetGame(); hideMenu(); });
  window.addEventListener('keydown',(e)=>{ if(e.key==='Escape' && !menu.hidden) hideMenu(); });
  const input={left:false,right:false,paused:false};
  window.addEventListener('keydown',(e)=>{ const k=e.key.toLowerCase(); if(k==='arrowleft'||k==='a') input.left=true; if(k==='arrowright'||k==='d') input.right=true; if(k==='p') input.paused=!input.paused; if(k==='r'){ state.stageCleared=false; if(state.mode==='level'){ buildLevel(); } else { state.wave=1; buildLevel(); } state.lives=3; state.balls=[new Ball(W/2,H-80,1)]; state.running=true; uiLives.textContent=state.lives; } });
  window.addEventListener('keyup',(e)=>{ const k=e.key.toLowerCase(); if(k==='arrowleft'||k==='a') input.left=false; if(k==='arrowright'||k==='d') input.right=false; });
  btnReset.addEventListener('click',()=>{ state.stageCleared=false; if(state.mode==='level'){ buildLevel(); } else { state.wave=1; buildLevel(); } state.lives=3; state.balls=[new Ball(W/2,H-80,1)]; state.running=true; uiLives.textContent=state.lives; }); btnPause.addEventListener('click',()=>{ input.paused=!input.paused; });
  function setMode(m){ state.mode=m; uiModeTag.textContent = (m==='endless')? 'Endless' : 'Nivel'; }
  function buildLevel(){ const comp = Levels.compute(state.mode, state.level, state.wave); const bp = comp.bricksBlueprint; state.bricks = bp.map(b=> new Brick(b.x,b.y,b.w,b.h,b.hp,b.requiresPierce,b.indestructible,b.opensCorridor)); state.barrierRows = comp.barrierRows; state.offsetX = comp.offsetX; state.corridor2Col = comp.corridor2Col; state.corridor2Opened=false; INIT_BALL_SPEED = comp.params.initSpeed; POWERUP_DROP_CHANCE = comp.params.dropChance; }
  function resetGame(){ state.paddle=new Paddle(); state.balls=[new Ball(W/2,H-60,1)]; state.powerUps=[]; state.score=0; state.lives=3; state.stageCleared=false; state.wave=1; buildLevel(); state.running=true; uiStage.textContent=(state.mode==='endless')?state.wave:state.level; uiScore.textContent=state.score; uiLives.textContent=state.lives; }
  function nextStage(){ if(state.mode==='level'){ state.stageCleared=true; state.running=false; } else { state.wave+=1; buildLevel(); state.balls=[new Ball(W/2,H-60,1)]; state.stageCleared=false; state.running=true; uiStage.textContent=state.wave; } }
  function spawnPowerUp(x,y){ const types=['multiball','wide','pierce','split']; if(Math.random()<POWERUP_DROP_CHANCE){ const t=types[Math.floor(Math.random()*types.length)]; state.powerUps.push(new PowerUp(x,y,t)); } }
  function checkStalemateSuccess(){ if(state.bricks.length===0) return false; const hasPlain = state.bricks.some(br => !br.indestructible && !br.requiresPierce); if(hasPlain) return false; const pierceActive = state.balls.some(b=>b.pierceTimer>0); return !pierceActive; }
  function updateTimersHUD(){ if(state.paddle.wideTimer>0){ wideBox.hidden=false; const pct = clamp(state.paddle.wideTimer / Math.max(0.0001, state.paddle.wideMax||8), 0, 1) * 100; wideFill.style.width = pct.toFixed(1)+'%'; } else { wideBox.hidden=true; } let maxPierce=0,maxPierceMax=0; for(const b of state.balls){ if(b.pierceTimer>maxPierce){ maxPierce=b.pierceTimer; maxPierceMax=Math.max(maxPierceMax, b.pierceMax||maxPierce); } } if(maxPierce>0){ pierceBox.hidden=false; const pct = clamp(maxPierce / Math.max(0.0001, maxPierceMax||5), 0, 1) * 100; pierceFill.style.width = pct.toFixed(1)+'%'; } else { pierceBox.hidden=true; } }
  function update(dt){ if(!state.running || input.paused) return; state.paddle.update(dt,input); for(const pu of state.powerUps) pu.update(dt); state.powerUps = state.powerUps.filter(p=>p.active); for(const pu of state.powerUps){ const pr=pu.rect(); const padd=state.paddle.rect(); const collide=!(pr.x+pr.w<padd.x||pr.x>padd.x+padd.w||pr.y+pr.h<padd.y||pr.y>padd.y+padd.h); if(collide){ sfx.power(); if(pu.type==='multiball'){ const newBalls=[]; for(const b of state.balls){ if(state.balls.length+newBalls.length>=MAX_BALLS) break; const nb1=new Ball(b.x,b.y,1); const nb2=new Ball(b.x,b.y,1); nb1.vx=b.vx*0.95; nb1.vy=-Math.abs(b.vy); nb2.vx=-b.vx*0.95; nb2.vy=-Math.abs(b.vy); nb1.color='#cde8ff'; nb2.color='#cde8ff'; newBalls.push(nb1,nb2);} state.balls.push(...newBalls); } else if(pu.type==='wide'){ state.paddle.wideTimer=Math.max(state.paddle.wideTimer,8); state.paddle.wideMax=Math.max(state.paddle.wideMax,8); } else if(pu.type==='pierce'){ for(const b of state.balls){ b.pierceTimer=Math.max(b.pierceTimer||0,5); b.pierceMax=Math.max(b.pierceMax||0,5); b.color='#ffc0cb'; } } else if(pu.type==='split'){ const splitBalls=[]; const original=state.balls.slice(0,Math.min(state.balls.length,6)); for(const b of original){ if(state.balls.length+splitBalls.length>=MAX_BALLS) break; for(let i=0;i<2;i++){ const nb=new Ball(b.x,b.y,1); const angle=randRange(-0.6,0.6); const speed=Math.hypot(b.vx,b.vy); nb.vx=Math.cos(angle)*speed; nb.vy=-Math.abs(Math.sin(angle)*speed); nb.color='#47e5bc'; splitBalls.push(nb);} } state.balls.push(...splitBalls); } pu.active=false; } } for(const ball of state.balls){ ball.update(dt); const padd=state.paddle.rect(); if(circleRectCollide(ball.x,ball.y,ball.r,padd.x,padd.y,padd.w,padd.h) && ball.vy>0){ const hitPos=(ball.x-padd.x)/padd.w-0.5; const angle=hitPos*Math.PI*0.7; const speed=Math.hypot(ball.vx,ball.vy); ball.vx=Math.sin(angle)*speed; ball.vy=-Math.abs(Math.cos(angle)*speed); ball.y=padd.y-ball.r-0.01; sfx.bounce(); } for(let i=state.bricks.length-1;i>=0;i--){ const br=state.bricks[i]; if(circleRectCollide(ball.x,ball.y,ball.r,br.x,br.y,br.w,br.h)){ if(br.indestructible && !(ball.pierceTimer>0)){ const dx=(ball.x-(br.x+br.w/2))/(br.w/2); const dy=(ball.y-(br.y+br.h/2))/(br.h/2); if(Math.abs(dx)>Math.abs(dy)) ball.vx=-ball.vx; else ball.vy=-ball.vy; sfx.bounce(); spawnParticles(ball.x,ball.y,'#9aa0a6',5); continue; } if(br.requiresPierce && !(ball.pierceTimer>0)){ const dx=(ball.x-(br.x+br.w/2))/(br.w/2); const dy=(ball.y-(br.y+br.h/2))/(br.h/2); if(Math.abs(dx)>Math.abs(dy)) ball.vx=-ball.vx; else ball.vy=-ball.vy; sfx.bounce(); spawnParticles(ball.x,ball.y,'#ffa500',6); continue; } if(!(ball.pierceTimer>0)){ const dx=(ball.x-(br.x+br.w/2))/(br.w/2); const dy=(ball.y-(br.y+br.h/2))/(br.h/2); if(Math.abs(dx)>Math.abs(dy)) ball.vx=-ball.vx; else ball.vy=-ball.vy; sfx.bounce(); } if(!br.indestructible){ br.hp-=1; uiScore.textContent=(state.score+=10); sfx.brick(); spawnParticles(ball.x,ball.y, br.requiresPierce?'#ffa500':'#7aa2ff', 10); if(br.hp<=0){ state.bricks.splice(i,1); if(br.opensCorridor && !state.corridor2Opened){ state.corridor2Opened=true; Levels.openSecondCorridor({ bricks:state.bricks, barrierRows:state.barrierRows, offsetX:state.offsetX, corridor2Col:state.corridor2Col, BRICK_W, BRICK_H, BRICK_GAP }); } spawnPowerUp(br.x+br.w/2-7, br.y+br.h/2-7); } } } } if(ball.pierceTimer>0){ ball.pierceTimer-=dt; if(ball.pierceTimer<=0){ ball.color='#f4f1c9'; } } if(ball.y-ball.r>H){ const idx=state.balls.indexOf(ball); if(idx!==-1) state.balls.splice(idx,1); if(state.balls.length===0){ state.lives-=1; sfx.lose(); uiLives.textContent=state.lives; if(state.lives>0){ buildLevel(); state.powerUps=[]; state.balls=[new Ball(W/2,H-80,1)]; state.running=true; state.stageCleared=false; uiStage.textContent=(state.mode==='endless')?state.wave:state.level; } else { state.running=false; } } } } if(state.bricks.length===0){ sfx.win(); nextStage(); } else if(checkStalemateSuccess()){ sfx.win(); nextStage(); } updateTimersHUD(); updateParticles(dt); }
  function draw(){ ctx.fillStyle='#0e1734'; ctx.fillRect(0,0,W,H); const grad=ctx.createLinearGradient(0,0,W,H); grad.addColorStop(0,'rgba(34,102,255,0.06)'); grad.addColorStop(1,'rgba(255,255,255,0.02)'); ctx.fillStyle=grad; ctx.fillRect(0,0,W,H); for(const br of state.bricks) br.draw(ctx); for(const pu of state.powerUps) pu.draw(ctx); state.paddle.draw(ctx); for(const ball of state.balls) ball.draw(ctx); drawParticles(ctx); if(!state.running){ ctx.fillStyle='#eaeaf0'; ctx.textAlign='center'; ctx.font='24px system-ui'; if(state.lives<=0){ ctx.fillText('Game Over', W/2, H/2 - 12);} else if(state.stageCleared && state.mode==='level'){ ctx.fillText('Nivel complet!', W/2, H/2 - 12);} else if(input.paused){ ctx.fillText('Pauză', W/2, H/2 - 12);} ctx.font='14px system-ui'; ctx.fillText('Apasă R pentru Reset sau deschide Meniul', W/2, H/2 + 18); } }
  let last=performance.now(); function loop(now){ const dt=Math.min(0.033,(now-last)/1000); last=now; update(dt); draw(); requestAnimationFrame(loop); }
  setMode('level'); resetGame(); requestAnimationFrame(loop);
})();
